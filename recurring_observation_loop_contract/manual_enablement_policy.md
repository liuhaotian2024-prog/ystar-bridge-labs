# Manual Enablement Policy

No recurrence, scheduler, daemon, live action, external action, persistence, brain writeback, or memory ingestion is enabled by L4.8.

Future enablement must remain manual until all checklist items are reviewed through governance and operator approval. Checklist statuses in this milestone are restricted to `not_started` and `defined_disabled`.

