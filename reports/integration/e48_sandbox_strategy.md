# E48 Sandbox Strategy

The scratch workspace is `/tmp/e48_gov_mcp_demo_codegrounded`. E48 overrides HOME, XDG_STATE_HOME, and LOCALAPPDATA when probing safe CLI status paths. Install remains blocked by default, and no real Claude/Cursor/Windsurf/OpenClaw config mutation occurred.
