# Open Gaps

- No single LLM reflection calculator was found. Reflection exists as ontology/counterfactual policy, metacognition design, and dream/consolidation machinery, but not as one standalone implemented calculator.
- ResidualLoopEngine live implementation status is unclear. Amendment 014 defines the loop, but current runtime status needs validation.
- L2 writeback hook wiring needs current validation. The partial receipt from 2026-04-20 says the module existed but hook wiring was missing at that time.
- Field gradient and metacognition runtime are partial/spec-level. `m_functor` validation exists, but full field navigation and metacognition sensitivity checks need later verification.
- DB contents were not inspected. Brain, memory, CIEU, and omission DBs remain metadata/path-only references.
- Exact multi-agent generalization beyond Aiden needs a later pass. Current capsule uses Aiden as the evidenced center and references team/multi-agent assets without creating per-agent brain profiles.
- `team_dna.md` is older and may conflict with newer actual team registry names.
- Dream auto-commit status is not asserted. Guardrails are specified, but current operational status needs validation.
