# Open Gaps

- No recurring mission-dashboard refresh loop exists yet.
- Agent-team proposal routing is dry-run only and cannot execute business work.
- Tool selection is limited to the existing governed read-only observation tool.
- CIEU events and residual deltas are fixtures only and are not persisted.
- Future live work still requires operator approval, sandboxing, rollback, and
  persistence gates.
